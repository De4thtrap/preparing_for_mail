
	### пользователи и группы ###
whoami											выводит имя пользователя
echo $USER										выводит имя пользователя
groups											список групп, в которых состоит пользователь
id												идентефикаторы пользователя и его групп
hostname										имя текущего сервера
useradd <name>									создать пользователя
	-m							создать для пользователя каталог
	-g <init_group>				название группы, в которую добавят пользователя
	-G <g1,g2,g3>				аналог -g для нескольких групп
groupadd <name>									создать группу
getent group									список групп
cat /etc/group									список групп
getent rasswd									список пользователей
cat /etc/passwd									список пользователей
usermod											редактирование пользователя
groupmod										редактирование группы
userdel											удаление пользователя
groupdel										удаление пользователя
passwd <user>									сменить пароль пользователя
su <user>										сменить текущего пользователя

	### файловая сиситема ###
ls												список файлов (подробнее / скрытые)
	-l							подробнее
	-la							в том числе скрытые
uname											название ОС
	-sr							версия ядра
	-a							подробнее (в том числе тип процессора)
df												disk free (информация о точках монтирования и свободном месте)
	-i							инфо про inode
	-h 							человекочитаемо
free											информация об оперативной памяти и swap
	-h							человекочитаемо
who												кто сейчас пользуется системой
env												переменные окружения
pwd												print working directory печатает текущий каталог
stat <file>										информация (дата изменеия, размер, inode, колво ссылок)
/dev/null										каталог чёрная дыра
/dev/zero										источник нулей
/dev/random, /dev/urandom						источники случайных чисел
tee	<file>										дублирует данные из stdin в stdout и файл
which <program>									адрес по которому лежит программа
cat <file>										выводит файл в терминал
cat > <file> вводим текст + ctrl+d				создали файл
touch <file>									создали файл
mkdir											создать каталог
	-p							можно сразу путь
cp <old> <new>									копировать 
	-r							рекурсивно (для директорий)

	### работа с процессами ###
^Z (Ctrl+z)										увести текущую программу в фон
bg												продолжить её выполнение в фоне
fg												достать программу из фона
jobs											посмотреть список фоновых задач
fg N											достать задачу номер N

grep <str>										выводит строки с подстрокой str
find:											найти в текущей директории
	-name						фильтр по имени
	-type						указать тип объекта который мы ищем d – директория, f – файл
	-mtime						фильтр по времени изменения. -2 – файлы моложе 2х дней
	-ls							вывод информации в стиле ls –l
	-exec <command> {} \;		для каждого найденного файла выполнить команду
top:											аля диспетчер задач
клавиши/ключи:
	d							раз в сколько секунд обновлть
	c							показать не название программ, а полный путь
	H							показать thread’ы
	M							отсортировать по памяти
	P							отсортировать по процессору
	i							скыть/показать idle процессы
	1							про каждое ядро отдельно




